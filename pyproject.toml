[project]
name = "subgenx"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Chris Paganon", email = "info@chrispaganon.com" }
]
requires-python = ">=3.12"
dependencies = [
    "whisperx>=3.8.5",
]

[project.scripts]
subgenx = "subgenx.cli:main"

[build-system]
requires = ["uv_build>=0.11.13,<0.12.0"]
build-backend = "uv_build"
