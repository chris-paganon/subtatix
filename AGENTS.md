This is a python uv project set up with uv init --package subgenx.

Run uv functions in the project root, outside the sandbox, I will approve commands

Don't export all functions in the **init**.py file unless they are ment to be called by a user of the package as an alternative/separate tool from the main cli tool.
